# MATLAB实现控制系统串联校正的频域设计

## 一、实验目的

（1）对给定系统设计满足频域或时域指标的串联校正装置；

（2）掌握频域法设计串联校正的方法；

（3）掌握串联校正环节对系统稳定性及过渡过程的影响。

## 二、实验原理及内容

利用MATLAB可以方便的画出Bode图并求出幅值裕量和相角裕量。将MATLAB应用到经典理论的校正方法中，可以方便的校验系统校正前后的性能指标。通过反复试探不同校正参数对应的不同性能指标，能够设计出最佳的校正装置。

### 1、串联超前校正

用频域法对系统进行超前校正的基本原理，是利用超前校正网络的相位超前特性来增大系统的相位裕量，以达到改善系统瞬态响应的目标。为此，要求校正网络最大的相位超前角出现在系统的截止频率（剪切频率）处。

串联超前校正的特点：主要对未校正系统中频段进行校正，使校正后中频段幅值的斜率为-20dB/dec，且有足够大的相位裕度；超前校正会使系统瞬态响应的速度变快，校正后系统的截止频率增大。这表明校正后，系统的频带变宽，瞬态响应速度变快，相当于微分效应；但系统抗高频噪声的能力变差。

用频率法对系统进行串联超前校正的一般步骤为：

1）根据稳态误差的要求，确定开环增益*K*。

2）根据所确定的开环增益*K*，画出未校正系统的波特图，计算未校正系统的相位裕度。

3）计算超前网络参数*α*和*τ*。

4）确定校正网络的转折频率。

5）画出校正后系统的波特图，验证已校正系统的相位裕度。

**【范例4-1】**给定系统如图4-1所示，试设计一个串联校正装置，使系统满足幅值裕量大于10分贝，相位裕量≥45°。

> [图片：图4-1 校正前系统]

图4-1 校正前系统

为了满足上述要求，试探地采用超前校正装置G~c~(s)，使系统变为图4-2的结构。

> [图片：图4-2 校正后系统]

图4-2 校正后系统

用下面的MATLAB语句得出原系统的幅值裕量与相位裕量。

```matlab
>> G = tf(100, [0.04, 1, 0]);
[Gw, Pw, Wcg, Wcp] = margin(G);
```

在命令窗口中显示如下结果：

```
Gw =                          Pw =
      Inf                        28.0243
Wcg =                         Wcp =
      Inf                        46.9701
```

可以看出，这个系统有无穷大的幅值裕量，并且其相位裕量γ＝28°，幅值穿越频率ω~cp~=47rad/sec。

引入一个串联超前校正装置：

$$G_{c}(s) = \frac{0.025s + 1}{0.01s + 1}$$

可以通过下面的MATLAB语句得出校正前后系统的Bode图如图4-3，校正前后系统的阶跃响应图如图4-4。其中ω~1~、γ~1~、*t~s~*~1~分别为校正前系统的幅值穿越频率、相角裕量、调节时间，ω~2~、γ~2~、*t~s~*~2~分别为校正后系统的幅值穿越频率、相角裕量、调节时间。

```matlab
>> G1 = tf(100, [0.04, 1, 0]);   % 校正前模型
G2 = tf(100 * [0.025, 1], conv([0.04, 1, 0], [0.01, 1]));   % 校正后模型

% 画伯德图，校正前用实线，校正后用短划线
bode(G1);
hold;
bode(G2, '--');   % 原文此处引号误写为 ′\--′，已修正

% 画时域响应图，校正前用实线，校正后用短划线
figure;
G1_c = feedback(G1, 1);
G2_c = feedback(G2, 1);
step(G1_c);
hold;
step(G2_c, '--');   % 原文此处引号误写为 ′\--′，已修正
```

> [图片：图4-3 校正前后系统的Bode图]

图4-3 校正前后系统的Bode图

> [图片：图4-4 校正前后系统的阶跃响应图]

图4-4 校正前后系统的阶跃响应图

可以看出，在这样的控制器下，校正后系统的相位裕量由28°增加到48°，调节时间由0.28s减少到0.08s。系统的性能有了明显的提高，满足了设计要求。

【范例4-2】已知单位负反馈传递函数$G_{o}(s) = \frac{K}{s^{2}(0.2s + 1)}$，试设计无源串联超前校正网络的传递函数，使系统的静态加速度误差系数*K~a~*=10，相角裕度γ>35°。

（1）*K~a~*=10

（2）绘制未校正系统的bode图，（γ=-30.5°，不稳定）

（3）设计串联超前校正装置，确定校正装置提供的相位超前量ϕ~m~（84°，采用两级超前校正网络实现，每一级为42°）

（4）确定校正网络的转折频率ω~1~和ω~2~，然后确定校正器的传递函数

> [图片：范例4-2 设计计算过程（含公式推导）]

> [图片：范例4-2 校正装置传递函数及相关参数]

> [图片：范例4-2 校正器传递函数]

> [图片：图4-5 校正前（蓝色）和校正后（红色）系统的Bode图]

图4-5 校正前（蓝色）和校正后（红色）系统的Bode图

> [图片：图4-6 校正前（蓝色）和校正后（绿色）系统的单位阶跃响应]

图4-6 校正前（蓝色）和校正后（绿色）系统的单位阶跃响应

### 2、串联滞后校正

滞后校正网络具有低通滤波器的特性，会使系统开环频率特性的中频和高频段增益降低和截止频率减小，从而有可能使系统获得足够大的相位裕度，它不影响频率特性的低频段。但是校正后系统的截止频率会减小，瞬态响应的速度要变慢。

单位反馈最小相位系统频率法设计串联滞后校正网络的步骤如下：

1）根据稳态性能要求，确定开环增益K；

2）利用已确定的开环增益，画出未校正系统对数频率特性曲线，确定未校正系统的截止频率、相位裕度和幅值裕度；

3）选择不同的ω~c~''，计算或查出不同的γ值，在伯特图上绘制γ(ω~c~'')曲线；

4）根据相位裕度要求，选择已校正系统的截止频率；

5）确定滞后网络参数b和T；

6）验算已校正系统的相位裕度和幅值裕度。

【范例4-3】已知单位负反馈系统$G_{0}(s) = \frac{K}{s(0.0625s + 1)(0.2s + 1)}$，设计要求该系统的相角裕度满足γ ≥ 50°，幅值裕度h ≥ 17dB，静态速度误差K~v~ = 40s^(-1)。求串联滞后校正装置的传递函数G~c~(s)。

> [图片：范例4-3 设计计算过程（含公式推导）]

> [图片：范例4-3 校正装置传递函数及相关参数]

> [图片：范例4-3 校正前后系统的Bode图及阶跃响应图]

## 三、【综合实践】

### 1、设单位负反馈系统的开环传递函数为：$\mathbf{G}_{\mathbf{0}}\mathbf{(s) =}\frac{\mathbf{K}}{\mathbf{s(s + 1)}}$，试设计校正装置*G~c~*(*s*)，使校正后系统满足如下指标：

（1）当r=t时，稳态误差e~ss~ ≤ 0.1；

（2）开环系统截止频率ω~c~ ≥ 6rad/s；

（3）相角裕度γ ≥ 60°；

完成下表：

| $G_{0}(s)$ | $G_{c}(s)$ | 校正前和校正后系统的Bode图 | 校正前和校正后系统的单位阶跃响应 |
| :---: | :---: | :---: | :---: |
| $\frac{10}{s(s + 1)}$ | $\frac{0.6159s + 1}{0.04422s + 1}$ |  |  |

### 2、设单位负反馈系统的开环传递函数为：$\mathbf{G}_{\mathbf{0}}\mathbf{(s) =}\frac{\mathbf{K}}{\mathbf{s(s + 1)(0.25s + 1)}}$，试设计滞后校正装置*G~c~*(*s*)，使校正后系统满足如下指标：

（1）当r=t时，稳态误差e~ss~ ≤ 0.2；

（2）相角裕度γ ≥ 45°；

完成下表：

| $G_{0}(s)$ | $G_{c}(s)$ | 校正前和校正后系统的Bode图 | 校正前和校正后系统的单位阶跃响应 |
| :---: | :---: | :---: | :---: |
| $\frac{5}{s(s + 1)(0.25s + 1)}$ | $\frac{16.39s + 1}{113.386s + 1}$ |  |  |

## 四、实验能力要求

（1）熟练掌握频率法设计控制系统串联超前/滞后校正网络的方法。

（2）熟练使用MATLAB编程完成控制系统串联超前/滞后校正设计，掌握函数find()的作用，并灵活运用。

（3）比较分析控制系统校正前后的各项性能指标，明确串联超前/滞后校正的作用。

（4）了解串联超前/滞后校正环节对系统稳定性及过渡过程的影响。

---

## 补充说明（由复述者添加，以帮助读者理解本文档）

（1）**关于代码引号修正**：文档范例4-1的MATLAB代码中，`bode(G2, ′\--′)` 和 `step(G2_c, ′\--′)` 使用了非标准的引号字符（U+2032或中文字符），在MATLAB中会报语法错误。本复述已将其全部修正为英文单引号 `'--'`。

（2）**关于综合实践表格中的G~c~(s)**：表格中给出的G~c~(s)（超前校正为0.6159s+1/0.04422s+1，滞后校正为16.39s+1/113.386s+1）是根据给定的性能指标经过设计计算后得到的参考结果。建议读者先按照原理部分的步骤（确定K→计算未校正裕度→确定校正参数→验算）自行推导验证，再与表格中的参考答案进行对比，而不是直接将其作为已知条件。

（3）**关于K值与表格的对应关系**：综合实践1的正文中，开环传递函数用K表示（$\frac{K}{s(s+1)}$），而表格中直接给出了$\frac{10}{s(s+1)}$。这是因为指标（1）要求稳态误差e~ss~≤0.1，对于单位负反馈系统，静态速度误差系数K~v~ = K（因$G(s)=\frac{K}{s(s+1)}$，K~v~ = lim~s→0~ s·G(s) = K），稳态误差e~ss~ = 1/K~v~ = 1/K ≤ 0.1，由此推出K ≥ 10，故表格中取K=10。同理，综合实践2中K=5来源于静态速度误差系数要求。本实验的表格直接给出了代入K值后的传递函数，简化了重复书写。
